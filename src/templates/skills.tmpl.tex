{% if resume.skills -%}
\section{Technical Skills}
 \begin{itemize}[leftmargin=0.15in, label={}]
    \small{\item{
			{% for category in resume.skills %}
			\textbf{% raw %}{{% endraw %}{{ category.name }}{% raw %}}{% endraw %}{: {{ category.skills | join(sep=', ') }}{% raw %}}{% endraw %} \\
			{% endfor %}
    }}
 \end{itemize}
 {% endif -%}